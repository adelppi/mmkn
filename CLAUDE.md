# CLAUDE.md

mmkn — グループ内で発生したお金の動きを記録し、その記録から「今、誰が誰にいくら送ればよいか」を導出するシステム。

<!-- 技術スタック・対応プラットフォーム・言語が決まったら 1〜2 行でここに書く。選定理由の正は docs/adr/。 -->

## 最初に読むもの

- `docs/RULES.md` … **docs に何かを書く前に必ず読む。** どこに何を書くかの配置ルール
- `docs/README.md` … 目次と「どの事実がどこにあるか」の一覧
- 機能の実装・変更前に、対象機能の `docs/domain/*.md` と関連 ADR を必ず読む
- `TEMPLATE.md` … このプロジェクトの土台になっている雛形（AI 駆動開発テンプレート）の説明。**進め方の参照用であり、mmkn の仕様ではない**

3 層の責務分離（WHAT = `features.md` / 振る舞い = `domain/` / 実現 = `adr/`）と「一次情報は 1 か所だけ」のルールは `docs/RULES.md` を正とする。ここには再掲しない。

**特に守ること:**

- `domain/` に**インフラ・ライブラリ・DB 構造を持ち込まない**
- `adr/` に**ドメインのルールを定義しない**（参照はする）

## 開発の進め方(イシュー駆動)

- **1 Issue = 1 ブランチ = 1 PR**。main へ直接コミットしない。
- ブランチ名: `feat/<issue番号>-<短い英語スラッグ>`。バグは `fix/`、ドキュメントは `docs/`、それ以外は `chore/`。
- マージは **squash merge**。PR タイトルがそのまま main のコミットメッセージになるため、PR タイトルは `feat: プラン作成を追加` の形式で書く。
- PR 本文に `Closes #<issue番号>` を書き、マージで Issue を自動クローズする。
- Issue・PR・コミットメッセージはすべて日本語。コミットメッセージは `feat:` `fix:` `docs:` `chore:` のプレフィックス + 日本語の要約。

GitHub 操作は GitHub MCP ツールで行う。`gh` CLI が使えるならそれでもよい。対象リポジトリは `git remote get-url origin` から解決する。

スラッシュコマンド:

| コマンド | 用途 |
|---|---|
| `/sync-docs` | 会話で決まったことを docs/ に反映する（配置を判定し、必要なら ADR も作る） |
| `/check-docs` | docs/ の整合性を検査する（配置ルール違反・リンク・重複・保留の同期） |
| `/create-issue <一文>` | 大まかな要求から質問で詳細を掘り下げ、Issue を起票 |
| `/implement-issue <番号>` | Issue を読み、ブランチ作成→実装→テスト→PR 作成まで実行 |

**仕様や技術方針を話し合ったら、その会話の終わりに `/sync-docs` を実行する。** docs に落ちていない決定は、次の会話では存在しないものとして扱われる。

## 要件定義の進め方

要件定義に専用のコマンドは持たない。**ユーザーと普通に会話して決め、`/sync-docs` で docs に落とす。**

書き溜まる順序の目安:

```
overview.md      何を・誰に・なぜ作るか
   ↓
features.md      何を作る／作らないか
   ↓
domain/*.md      どう動くか（機能ごと）
   ↓
adr/XXXX-*.md    どう作るか（技術を決めたとき随時）
   ↓
Issue → 実装
```

会話中に決まらなかったことは `[保留]` を付けて `docs/open-questions.md` に登録する。**AI が勝手に決めない。**

## コーディング規約(正は各 ADR)

<!--
技術方針を決めたら /sync-docs が ADR を作り、この節に ADR 番号付きで反映する。
最低限そろえるべき決定は次の 4 つ。

- **ディレクトリ構成・依存方向**: `adr/XXXX`
- **データ層**: `adr/XXXX`（外部サービスの SDK を import してよい場所を 1 か所に閉じ込める）
- **テスト**: `adr/XXXX`（何を自動テストで守り、何をレビュー・手動で守るか）
- **UI**: `adr/XXXX`
-->

## docs の更新ルール

- 仕様(状態・条件・タイミング)を変えたら `domain/` を同じ PR で更新する。コードだけ変えて docs を放置しない。
- 技術的な意思決定をしたら ADR に記録する。既存 ADR を覆す場合は旧 ADR を「廃止(superseded)」にする。
- 未確定事項は勝手に決めない。`[保留]` を付けて `open-questions.md` に登録し、ユーザーに確認する。
- 記述には `[確定]` / `[提案]` / `[保留]` のステータス印を付ける(定義は `docs/RULES.md`)。

## 品質チェック

- PR 作成前にテスト・lint・typecheck を通す(存在するもののみ)。落ちたまま PR を作らない。

<!-- 自動テストで守らないと決めた領域(例: 権限境界)があれば、「PR で必ずレビュー確認する観点」としてここに列挙する。 -->

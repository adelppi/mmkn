/**
 * 参加コードを生成するポート（`docs/domain/group.md`「実現方式」）。
 *
 * ドメインが要求するのは「グループごとに一意」「共有リンクに埋め込める」
 * 「他のコードから推測できない」までで、文字種や長さは規定していない。形式は
 * `docs/adr/0002-invite-code.md` を正とする。
 *
 * **衝突は起きない前提にしない**（`docs/adr/0002`）。生成した値が既存のグループと
 * 重なっていた場合に生成し直すのは、呼び出し側（グループ作成のユースケース）の責務。
 */
export type InviteCodeGenerator = {
  next(): string
}

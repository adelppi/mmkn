# 未確定論点（決定バックログ） — mmkn

本文に `[保留]` を付けた記述の集約先。**ここが「まだ決まっていないこと」の一次情報。**

## 運用ルール

- 本文に `[保留]` を書いたら、必ずここにも 1 行足す。逆に、ここにある項目は本文のどこかに `[保留]` として存在する。
- 決着したら、**本文の `[保留]` を `[確定]` に更新してから**、ここの項目を「解決済み」へ移す。本文を直さずここだけ消さない。
- 技術的な決定として決着したら ADR に記録し（`/sync-docs`）、ADR 番号を「決定の記録」列に書く。
- **AI が勝手に決めない。** 判断がいる場面で該当項目に当たったら、実装を止めて人間に確認する。

## 未決の論点

番号は退番する。解決済みへ移した番号を再利用しない。

| # | 論点 | 関連文書 | 決めるべき時期 | 備考 |
|---|---|---|---|---|
| 15 | **認証のメールをどう送るか。** `adr/0012` はメール確認とパスワード再設定をメールに依存させているが、認証基盤の既定の送信サービスは**組織のチームに属さないアドレスへの配信を拒否する**。カスタム SMTP を用意するまで、利用者は 1 人も登録できない | `adr/0012-login.md`「着手時の確認結果」／`overview.md`「前提」 | **アカウント作成（`features.md` #11）を実装する前。** これが決まらないと画面を作っても誰も通れない | Issue #9 の確認で判明（2026-08-13）。**外部サービスを 1 つ増やす決定**であり、`overview.md`「運用コストも無料枠に収める」に触れる。無料枠のある送信サービスを使うか、前提の側を見直すか、メールに依存しない認証（`adr/0012` の代替案にあるパスキー等）へ寄せるかを含む。AI が決めない |
| 14 | **E2E が使う認証基盤をどうするか。** `adr/0011` は「E2E は本番に対して回さない」を DB についてだけ決めており、認証基盤には何も言っていない。CI 内でアプリを起動する以上アプリは認証基盤に繋がる | `adr/0011-ci-and-release.md`「main へのマージ時に走らせるもの」／`adr/0010-testing.md`／`adr/0012-login.md` | **E2E を作るとき。** それまで CI に認証の環境変数は要らない | Issue #9 の環境変数の整理で判明（2026-08-13）。**DB には「使い捨ての Postgres」があるが、認証基盤には対応する逃げ道が無い。** 本番に向ければ E2E が本物のアカウントを作り、`adr/0015`（バックアップ）と `overview.md`「前提」（無料枠。プロジェクトを増やすか）の両方に触れる。未決 #12 と一緒に見るのが早い |
| 13 | **最小単位を持たない通貨コードを扱うか。** ISO 4217 の現行の一覧には最小単位が `N.A.` のコードが 13 件ある（貴金属・特別引出権・テスト用など）。`domain/money.md` は「最小単位の桁数も ISO 4217 に従う」と定めているが、これらに従うべき桁数が無い | `domain/money.md`「扱う通貨」／`adr/0013`「取得元の形」 | **通貨表の生成器を作るとき**（`adr/0013`）。アカウント作成より前 | Issue #9 の確認で判明（2026-08-12）。金額を「整数 + 通貨」で持つ以上、桁数の無いコードは金額として解釈できない。**表から落とすか、入力候補に出さないだけにするかはドメインの決定**であり、AI が決めない。`adr/0013` は「知らない通貨は 2 桁とみなす」型の既定値を禁じている |

未決が無いことは「これ以上決めることが無い」という意味ではない。新しく決まらなかったことが出たら、本文に `[保留]` を付けてここに 1 行足す。

### 「実装着手時に必ず確かめること」の移動先

決定済みだが前提の確認が要るものは、**未決ではない**ため、この文書ではなく `docs/operations.md`「実装着手時に必ず確かめること」に置く（`RULES.md` §6・§9）。

## 解決済み

| 論点 | 決定 | 決定の記録 | 決定日 |
|---|---|---|---|
| 無料枠に自動バックアップが無いことが分かった。バックアップをどうするか（#12） | **バックアップを持たない。** 独自の定期ダンプも有料プランへの移行も行わず、**復旧できない範囲（＝登録されたすべての記録）を明示して受け入れる**。無料枠の前提（`overview.md`）を保ったまま採れる手段が他に無いため。定期ダンプは、失敗に気づく手段を持てない（`adr/0014` が監視を持たない）以上「あるつもりで壊れている」状態を作るため採らない。**この決定を見直す契機を ADR に明記した** | `adr/0015-backup.md` | 2026-08-13 |
| ISO 4217 から削除された通貨の扱い（#11） | **記録が存在する限り、現行の通貨と何も変わらない扱いをする。** 桁数は削除時点のものを使い続け、新しい記録にも選べる（入力候補に出すのは現行通貨と、そのグループに既に記録がある通貨だけ）。新規入力を一律に塞がないのは、収支が残った通貨で Transfer を登録できないと清算を閉じられなくなるため。表の側は行を消さず廃止の印を立てる | `domain/money.md`「廃止された通貨」／`adr/0013-currency-data.md` | 2026-08-12 |
| Discord でモーダルを使うか（「すべての Interaction で一律 deferred」が Discord の仕様と両立していなかった） | **使う。** `MODAL_SUBMIT` を扱う種別に含める（これ自体は defer できる）。一律 deferred は「メッセージで応答するもの」に限り、`PING`・`AUTOCOMPLETE`・モーダルを開く応答を例外として明示する。モーダルの組み立てに永続化を混ぜない。**「使わない」とした 2026-08-12 の判断は同日に撤回**（理由としていた「コールドスタートのリスクを戻す」が誤りであったため。`adr/0006`「モーダル」の引用を正とする） | `adr/0006-discord-http-interactions.md` | 2026-08-12 |
| v1 のスコープ | 13 機能すべてを含める。段階リリースをしない。実装順序は Issue と Milestone で持つ | `features.md`「v1 のスコープ」 | 2026-08-12 |
| mmkn 自身のログイン方式 | メールアドレス + パスワード。Supabase Auth に委譲する。メール確認を必須にし、再設定はメール経由。**うち「メールで確認・再設定する」の部分は 2026-08-13 に前提が外れた**（既定の送信サービスがチーム外に配信しない。未決 #15 で再検討。識別子と認証手段の決定そのものは有効） | `adr/0012-login.md` | 2026-08-12 |
| 清算案の送金記録で送り手を誰にできるか（Discord のボタンだけ押下者に固定していた） | ボタンは同じユースケースへのショートカット。押下者以外を送り手とする登録は通常の送金の記録で行え、Discord にもそれを用意するため機能の差にならない | `adr/0006-discord-http-interactions.md` | 2026-08-12 |
| 前提条件を満たさなかったときに起きること（全操作で未定義だった） | 失敗し、データは変わらず、理由が伝わる。「存在しない」と「Member でない」と「未ログイン」を区別する。前提として Group の識別子に推測不能性を要求する | `domain/group.md`「前提条件を満たさなかったとき」 | 2026-08-12 |
| ログアウトと退会を持つか | ログアウトは持つ。退会（アカウント削除）は持たない（Member が指す先が壊れるため） | `domain/group.md`／`features.md`「mmkn が持たないもの」 | 2026-08-12 |
| 記録の「登録された前後関係」の表し方 | 登録日時を属性として持つ。`Clock` ポートから受け取り、編集では取り直さない | `domain/record.md`「記録の並び」／`adr/0008` | 2026-08-12 |
| 文字列の入力制約 | グループ名 50 / 表示名 20 / User の名前 20 / 内容 100 文字。前後の空白は落とす。内容だけ空を許す | `domain/group.md`／`domain/record.md` | 2026-08-12 |
| グループ名・既定通貨・表示名の同時変更 | 後勝ちで置き換える。Group には楽観ロックを持たせない | `domain/group.md`「境界・例外ケース」／`adr/0005` | 2026-08-12 |
| ISO 4217 の通貨一覧と最小単位の入手方法 | 公表データから生成した表をドメイン層にコミットする。実行時にも取得しない。追随は週次の定期実行が差分を PR にして行う。**「手で書き写す」とした 2026-08-12 の判断は同日に撤回**（追随の契機が人の記憶になるため） | `adr/0013-currency-data.md` | 2026-08-12 |
| ログと障害の気づき方 | 標準出力への構造化ログのみ。監視サービス・通知を持たない。記録の中身はログに出さない | `adr/0014-logging.md` | 2026-08-12 |
| バックアップと、運用コストの前提 | 運用コストも無料枠に収めることを `overview.md` に確定。バックアップはホスティングの標準機能に任せ、復旧できない範囲を明示する。**うち「標準機能に任せる」の部分は 2026-08-12 に前提が外れ、2026-08-13 に撤回した**（無料枠に自動バックアップが無い。上段「バックアップをどうするか（#12）」の行を正とする。「復旧できない範囲を明示する」と無料枠の前提そのものは有効） | `overview.md`「前提」／`adr/0015-backup.md` | 2026-08-12 |
| 発生日のタイムゾーン（海外で記録したときの「今日」） | タイムゾーンを持たない。入力された日付をそのまま保持する。入力欄の初期値だけ操作者の手元の「今日」を使う | `domain/record.md`「発生日」 | 2026-08-11 |
| 清算案の送金が、押した時点の清算案に無かったとき | Transfer を登録せず、清算案が変わったことを伝える。Discord では `UPDATE_MESSAGE` で最新の案に描き直す | `domain/settlement.md`／`adr/0006` | 2026-08-11 |
| 清算案が示す額の一部だけを送ったときの記録方法 | ボタン導線は全額専用。部分送金は金額を入力する通常の登録で行い、残額は収支から自動的に出る | `domain/settlement.md`「清算案の送金を記録する」 | 2026-08-11 |
| 同じ場に 2 人が同時に別の Group を対応づけたとき | 常に後勝ちで置き換える。記録の競合とは扱いを分ける（失われるのが対応先だけで、やり直せるため） | `domain/group.md`「境界・例外ケース」 | 2026-08-11 |
| 外部アカウントが既に別の User に連携済みのとき | 連携を失敗させ、連携先は移さない。付け替えは元の User が解除してから | `domain/group.md`「外部アカウントを連携する」 | 2026-08-11 |
| 二重に参加したときの表示名 | 反映しない。表示名を変えるのは「表示名を変更する」の責務 | `domain/group.md`「グループに参加する」 | 2026-08-11 |
| 表示名を変えたときの過去の記録の表示 | 過去の記録も新しい表示名で表示する。記録時点の表示名は保持しない | `domain/group.md`「表示名を変更する」 | 2026-08-11 |
| Transfer が「内容」を持つか | 持たない | `domain/record.md`「Transfer」 | 2026-08-11 |
| テストの道具と範囲 | Vitest / Playwright / Storybook。単体は対象の隣、層をまたぐものだけ `tests/`。実 DB はローカル Postgres。カバレッジの数値目標は持たない | `adr/0010-testing.md` | 2026-08-11 |
| クライアント間の整合をどう固定するか | 同じシナリオを Web と Discord の両方から流し、できた記録が一致することを検証する | `adr/0010-testing.md` | 2026-08-11 |
| Discord のスラッシュコマンド登録の走らせ方（`adr/0006` が両論併記のままだった） | main マージ時に GitHub Actions で、定義に差分があるときだけ登録する | `adr/0011-ci-and-release.md` | 2026-08-11 |
| マイグレーションの適用のタイミングと戻し方 | main マージ時に GitHub Actions で適用し、失敗したらデプロイしない。前方のみとし、壊す変更は 2 リリースに分ける | `adr/0011-ci-and-release.md` | 2026-08-11 |
| Preview デプロイと、その DB | Preview を持たない。DB が 1 つしか無い構成で、未マージのコードが本番データに到達する経路を作らない | `adr/0011-ci-and-release.md` | 2026-08-11 |
| 概念とコード上の名前の対応 | `glossary.md` に索引として持つ。定義は再掲せず `domain/` へリンクする | `docs/glossary.md`／`RULES.md` §10 | 2026-08-11 |
| 「提供価値」に既存の代替手段との比較を持つか | 持たない。比較を根拠に意思決定を変える場面がないため | `overview.md`「提供価値」 | 2026-08-11 |
| ユースケースの出力と失敗の返し方 | 戻り値で DTO を返し、失敗は `Result` 型で表す。出力ポートは持たない。失敗はユースケースごとのタグ付き union | `adr/0008-layer-internals.md` | 2026-08-11 |
| 永続化とトランザクションの単位 | 集約単位のリポジトリ。トランザクションは実装の中に閉じ、ユースケースは知らない | `adr/0008-layer-internals.md` | 2026-08-11 |
| 識別子の生成場所と、ドメインの基底クラスの要否 | ユースケースがポート経由で生成し、ファクトリは受け取るだけ。基底クラスは作らない（ドメインイベントを持つようになったら再検討） | `adr/0008-layer-internals.md` | 2026-08-11 |
| 楽観ロックのバージョンの置き場 | ドメインのエンティティに持たせず、ユースケースの入出力とポートで受け渡す | `adr/0005-data-access-and-authorization.md` | 2026-08-11 |
| アダプタ層の内部構造（Controller と Presenter の関係） | 対にして両方持つ。Presenter は Controller が呼ぶ純関数で、ユースケースからは呼ばれない | `adr/0008-layer-internals.md` | 2026-08-11 |
| 依存の組み立て方 | 合成ルートの関数 1 つをリクエストごとに呼ぶ。DI コンテナのライブラリは使わない | `adr/0008-layer-internals.md` | 2026-08-11 |
| セッション読み取りの置き場 | インフラ層に置き、cookie へのアクセス手段はアプリ層から注入する | `adr/0008-layer-internals.md`「セッションの読み取り」 | 2026-08-11 |
| Discord の返信の可視性（初版の記述が Discord の仕様と矛盾していた） | defer を開始する単位ごとに宣言し、defer 時に使う。失敗もその可視性で返る。「返信ごとに指定する」は撤回 | `adr/0006-discord-http-interactions.md` | 2026-08-11 |
| Web の画面構成 | Container/Presentational に分ける。失敗はビューモデルのタグとして表す。Container はデータ取得の単位で分ける | `adr/0009-web-ui.md` | 2026-08-11 |
| UI ライブラリ | shadcn/ui（Tailwind CSS + Radix）。部品はアプリ層に置く | `adr/0009-web-ui.md` | 2026-08-11 |
| フォームの実装方式とクライアント側の入力検査の範囲 | Server Action の戻り値をフォームの状態とし、状態管理のライブラリを入れない。クライアントの検査はブラウザ標準の入力属性まで | `adr/0009-web-ui.md` | 2026-08-11 |
| 成功の見方の言語化（#1） | 定量指標を持たない。設計判断で迷ったときの軸はターゲットに置く | `overview.md`「成功の見方」 | 2026-08-10 |
| ディレクトリ構造と命名規則（#4） | トップレベルを層で切り、`src/domain/` の直下を `docs/domain/` の分割に対応させる | `adr/0004-layers-and-dependencies.md` | 2026-08-10 |
| マイグレーションの運用（#5） | 生成した SQL をリポジトリにコミットし、デプロイ手順で適用する。本番に push 型は使わない。テーブル設計そのものは論点ではなく、`domain/` から起こす実装作業 | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| ORM の選定（#6） | Drizzle | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| 外部サービスから来た操作の対象 Group をどう解決するか（#7） | 「場」と Group の対応を持つ。1 つの場に対応する Group は 1 つ、1 つの Group は複数の場に対応してよい。変更・解除できる。Discord では場をチャンネルとする | `domain/group.md`「Group と外部サービスの場」／`adr/0006` | 2026-08-10 |
| 3 秒を超える処理の扱い（#8） | 一律に deferred response を返し、本文は follow-up で送る。**「すべての Interaction で」の部分は 2026-08-12 に撤回**（`PING`・`AUTOCOMPLETE`・モーダルを開く応答は defer できない。上段の「Discord でモーダルを使うか」の行を正とする） | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| 複数ステップ操作の中間状態（#9） | 中間状態を持たない。モーダル 1 枚かセレクト 1 回で確定させ、確定用のボタンを別に置かない。モーダルとセレクトを直列に繋がない（2026-08-12 にモーダルを追加） | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| 1 User に連携できる外部アカウントの数と解除の可否（#10） | 1 サービスにつき 1 つ。連携は解除できる。付け替えは解除してから連携し直す | `domain/group.md`「User と外部アカウント」 | 2026-08-10 |
| 技術スタック（#2） | TypeScript / Next.js App Router / Vercel / Supabase Postgres / Supabase Auth。Discord は常駐 Bot を持たず HTTP Interactions で接続する | `adr/0003-tech-stack.md` | 2026-08-10 |
| 層の分離をどう表現し、どう守るか | 単一パッケージ内でディレクトリとして分け、依存方向を既製ツールで CI 検査する | `adr/0004-layers-and-dependencies.md` | 2026-08-10 |
| 認可をどこに置くか（RLS を正本にするか） | 正本はドメイン層。RLS は全テーブルで有効化するが「誰も通さない」ポリシーだけを置き、業務ルールは書かない | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| データアクセスの経路 | `DATABASE_URL` + ORM（Supavisor の transaction mode）。Supabase SDK 経由のクライアント直接アクセスは使わない | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| 同時書き込みの競合対策 | 記録単位の楽観ロック。競合したら失敗させ、自動で再試行しない | `domain/record.md`「同じ記録に同時に手が入ったとき」／`adr/0005` | 2026-08-10 |
| JS 無効時の動作を要件にするか | しない。Server Actions は採用するが、JS 無効時の動作は保証すべき要件として扱わない | `adr/0003-tech-stack.md` | 2026-08-10 |
| User の同一性をどう確立するか（#3） | mmkn 自身のアカウントを主とし、外部アカウントは連携として紐づける。連携はログイン済みの Web から OAuth で行い、Supabase Auth に委譲する | `domain/group.md`「User と外部アカウント」／`adr/0007` | 2026-08-10 |
| Discord の返信にボタンを添えて操作を実行するか | する。清算案に「送金した」ボタンを添え、押下で Transfer を登録する。金額は登録時点で導出し直す | `features.md` #10 ／`domain/settlement.md`「清算案の送金を記録する」／`adr/0006` | 2026-08-10 |
| Discord の返信の可視性 | 記録の登録結果・収支・清算案は公開。入力途中は実行者のみ。**「返信ごとに指定できる構造にする」の部分は 2026-08-11 に撤回**（上段の行を正とする） | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| Payment の端数を誰に寄せるか | 負担者を Member の変わらない同一性による決定的な順序で並べ、先頭から 1 最小単位ずつ配る。表示順とは無関係 | `domain/record.md`「負担額の配分」 | 2026-08-10 |
| 清算案の送金件数を「最小」と断定するか | 断定しない。「できるだけ少なくするが、常に最小であることは保証しない」とする | `domain/settlement.md`「清算案」 | 2026-08-10 |
| グループ作成者の表示名をどう決めるか | User の名前を初期値として自動設定し、後から変更できる | `domain/group.md`「グループを作成する」 | 2026-08-10 |
| `domain/` の文書分割 | グループ / 金額と通貨 / 記録 / 収支と清算案 の 4 文書に分ける | `docs/README.md` 構成ツリー | 2026-08-10 |
| ターゲット | 旅行・パーティーなど、その場限りのイベントで一時的に集まったグループ | `overview.md`「ターゲット」 | 2026-08-10 |
| プラットフォーム | スマホ前提の Web アプリ。加えて Discord アプリとしても同機能を提供する | `overview.md`「前提」 | 2026-08-10 |
| 対応地域・言語 | 日本語のみ・日本国内が中心。ただし海外旅行で発生する外貨は扱う | `overview.md`「前提」 | 2026-08-10 |
| 収益・開発体制 | 無料・個人開発 | `overview.md`「前提」 | 2026-08-10 |
| User が持つ属性 | グループをまたいで変わらない同一性と、名前を持つ | `domain/group.md`「User の属性」 | 2026-08-10 |
| 参加コードを手入力させるか | させない。共有リンクに埋め込んで渡す。人が読み上げられる必要はない | `domain/group.md`「Group の属性」 | 2026-08-10 |
| 参加コードの形式・桁数 | cuid2 の既定設定（英小文字と数字・24 文字）で生成する | `adr/0002-invite-code.md` | 2026-08-10 |
| グループ名・既定通貨を後から変更できるか | どちらも変更できる。既定通貨を変えても過去の記録は変わらない | `domain/group.md`「グループ設定を変更する」 | 2026-08-10 |
| 対応する通貨の範囲と最小単位 | ISO 4217 の現行通貨。最小単位の桁数も ISO 4217 に従う | `domain/money.md`「扱う通貨」 | 2026-08-10 |
| 金額の上限 | 1 件の記録につき、その通貨の最小単位で 1,000,000,000 を上限とする。合計には上限を設けない | `domain/money.md`「金額の上限」 | 2026-08-10 |
| Payment の「内容」は必須か任意か | 任意。空でもよい | `domain/record.md`「Payment の属性」 | 2026-08-10 |
| 記録の並び順 | 発生日の新しい順。発生日が同じものは、後から登録したものを先に置く | `domain/record.md`「記録の並び」 | 2026-08-10 |
| 発生日に未来の日付を許すか | 許す。収支の計算は変わらない | `domain/record.md`「発生日」 | 2026-08-10 |
| 送金件数を「できるだけ少なく」する求め方 | 収支の絶対値が最大の負と最大の正を順に相殺する貪欲法 | `adr/0001-settlement-greedy.md` | 2026-08-10 |

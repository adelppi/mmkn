# 未確定論点（決定バックログ） — mmkn

本文に `[保留]` を付けた記述の集約先。**ここが「まだ決まっていないこと」の一次情報。**

## 運用ルール

- 本文に `[保留]` を書いたら、必ずここにも 1 行足す。逆に、ここにある項目は本文のどこかに `[保留]` として存在する。
- 決着したら、**本文の `[保留]` を `[確定]` に更新してから**、ここの項目を「解決済み」へ移す。本文を直さずここだけ消さない。
- 技術的な決定として決着したら ADR に記録し（`/sync-docs`）、ADR 番号を「決定の記録」列に書く。
- **AI が勝手に決めない。** 判断がいる場面で該当項目に当たったら、実装を止めて人間に確認する。

## 未決の論点

番号は退番する。解決済みへ移した番号を再利用しない。

| # | 論点 | 関連文書 | 決めるべき時期 | 備考 |
|---|---|---|---|---|
| — | **現時点で未決の論点はない。** [確定] | | | 新たに `[保留]` を書いたら、次の番号（11 以降）でここに足す |

### 実装着手時に必ず確かめること

決定済みだが、**前提が外れると影響が大きく、確認が安いもの。** 未決ではないので上の表には置かない。

| 確認すること | 外れたときの影響 | 記録 |
|---|---|---|
| 認証基盤が保存する Discord のユーザー ID と、スラッシュコマンドで届くユーザー ID が同一か | Discord 側が丸ごと動かない | `adr/0007` |
| Drizzle が Supavisor の transaction mode で動くか（prepared statement 無効化込み） | 本番でだけ動かない | `adr/0005` |
| 認証基盤で最後の 1 つの identity を削除したときの挙動 | 連携解除でアカウントごと消える可能性 | `adr/0007` |
| ホスティング環境で、応答を返したあとの処理継続が Discord の follow-up に間に合うか | 一律 deferred が成立せず、Discord の返信が届かない | `adr/0003`・`adr/0006` |

## 解決済み

| 論点 | 決定 | 決定の記録 | 決定日 |
|---|---|---|---|
| ユースケースの出力と失敗の返し方 | 戻り値で DTO を返し、失敗は `Result` 型で表す。出力ポートは持たない。失敗はユースケースごとのタグ付き union | `adr/0008-layer-internals.md` | 2026-08-11 |
| 永続化とトランザクションの単位 | 集約単位のリポジトリ。トランザクションは実装の中に閉じ、ユースケースは知らない | `adr/0008-layer-internals.md` | 2026-08-11 |
| 識別子の生成場所と、ドメインの基底クラスの要否 | ユースケースがポート経由で生成し、ファクトリは受け取るだけ。基底クラスは作らない（ドメインイベントを持つようになったら再検討） | `adr/0008-layer-internals.md` | 2026-08-11 |
| 楽観ロックのバージョンの置き場 | ドメインのエンティティに持たせず、ユースケースの入出力とポートで受け渡す | `adr/0005-data-access-and-authorization.md` | 2026-08-11 |
| アダプタ層の内部構造（Controller と Presenter の関係） | 対にして両方持つ。Presenter は Controller が呼ぶ純関数で、ユースケースからは呼ばれない | `adr/0008-layer-internals.md` | 2026-08-11 |
| 依存の組み立て方 | 合成ルートの関数 1 つをリクエストごとに呼ぶ。DI コンテナのライブラリは使わない | `adr/0008-layer-internals.md` | 2026-08-11 |
| セッション読み取りの置き場 | インフラ層に置き、cookie へのアクセスはアプリ層から注入する | `adr/0008-layer-internals.md` | 2026-08-11 |
| Discord の返信の可視性（初版の記述が Discord の仕様と矛盾していた） | defer を開始する単位ごとに宣言し、defer 時に使う。失敗もその可視性で返る。「返信ごとに指定する」は撤回 | `adr/0006-discord-http-interactions.md` | 2026-08-11 |
| Web の画面構成 | Container/Presentational に分ける。失敗はビューモデルのタグとして表す。Container はデータ取得の単位で分ける | `adr/0009-web-ui.md` | 2026-08-11 |
| UI ライブラリ | shadcn/ui（Tailwind CSS + Radix）。部品はアプリ層に置く | `adr/0009-web-ui.md` | 2026-08-11 |
| フォームの実装方式とクライアント側の入力検査の範囲 | Server Action の戻り値をフォームの状態とし、状態管理のライブラリを入れない。クライアントの検査はブラウザ標準の入力属性まで | `adr/0009-web-ui.md` | 2026-08-11 |
| 成功の見方の言語化（#1） | 定量指標を持たない。設計判断で迷ったときの軸はターゲットに置く | `overview.md`「成功の見方」 | 2026-08-10 |
| ディレクトリ構造と命名規則（#4） | トップレベルを層で切り、`src/domain/` の直下を `docs/domain/` の分割に対応させる | `adr/0004-layers-and-dependencies.md` | 2026-08-10 |
| マイグレーションの運用（#5） | 生成した SQL をリポジトリにコミットし、デプロイ手順で適用する。本番に push 型は使わない。テーブル設計そのものは論点ではなく、`domain/` から起こす実装作業 | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| ORM の選定（#6） | Drizzle | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| 外部サービスから来た操作の対象 Group をどう解決するか（#7） | 「場」と Group の対応を持つ。1 つの場に対応する Group は 1 つ、1 つの Group は複数の場に対応してよい。変更・解除できる。Discord では場をチャンネルとする | `domain/group.md`「Group と外部サービスの場」／`adr/0006` | 2026-08-10 |
| 3 秒を超える処理の扱い（#8） | すべての Interaction で一律に deferred response を返し、本文は follow-up で送る | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| 複数ステップ操作の中間状態（#9） | 中間状態を持たない。セレクト 1 回で確定させ、確定用のボタンを別に置かない | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| 1 User に連携できる外部アカウントの数と解除の可否（#10） | 1 サービスにつき 1 つ。連携は解除できる。付け替えは解除してから連携し直す | `domain/group.md`「User と外部アカウント」 | 2026-08-10 |
| 技術スタック（#2） | TypeScript / Next.js App Router / Vercel / Supabase Postgres / Supabase Auth。Discord は常駐 Bot を持たず HTTP Interactions で接続する | `adr/0003-tech-stack.md` | 2026-08-10 |
| 層の分離をどう表現し、どう守るか | 単一パッケージ内でディレクトリとして分け、依存方向を既製ツールで CI 検査する | `adr/0004-layers-and-dependencies.md` | 2026-08-10 |
| 認可をどこに置くか（RLS を正本にするか） | 正本はドメイン層。RLS は全テーブルで有効化するが「誰も通さない」ポリシーだけを置き、業務ルールは書かない | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| データアクセスの経路 | `DATABASE_URL` + ORM（Supavisor の transaction mode）。Supabase SDK 経由のクライアント直接アクセスは使わない | `adr/0005-data-access-and-authorization.md` | 2026-08-10 |
| 同時書き込みの競合対策 | 記録単位の楽観ロック。競合したら失敗させ、自動で再試行しない | `domain/record.md`「同じ記録に同時に手が入ったとき」／`adr/0005` | 2026-08-10 |
| JS 無効時の動作を要件にするか | しない。Server Actions は採用するが、JS 無効時の動作は保証すべき要件として扱わない | `adr/0003-tech-stack.md` | 2026-08-10 |
| User の同一性をどう確立するか（#3） | mmkn 自身のアカウントを主とし、外部アカウントは連携として紐づける。連携はログイン済みの Web から OAuth で行い、Supabase Auth に委譲する | `domain/group.md`「User と外部アカウント」／`adr/0007` | 2026-08-10 |
| Discord の返信にボタンを添えて操作を実行するか | する。清算案に「送金した」ボタンを添え、押下で Transfer を登録する。金額は登録時点で導出し直す | `features.md` #10 ／`domain/settlement.md`「清算案の送金を記録する」／`adr/0006` | 2026-08-10 |
| Discord の返信の可視性 | 記録の登録結果・差額・清算案は公開。入力途中は実行者のみ。**「返信ごとに指定できる構造にする」の部分は 2026-08-11 に撤回**（上段の行を正とする） | `adr/0006-discord-http-interactions.md` | 2026-08-10 |
| Payment の端数を誰に寄せるか | 負担者を Member の変わらない同一性による決定的な順序で並べ、先頭から 1 最小単位ずつ配る。表示順とは無関係 | `domain/record.md`「負担額の配分」 | 2026-08-10 |
| 清算案の送金件数を「最小」と断定するか | 断定しない。「できるだけ少なくするが、常に最小であることは保証しない」とする | `domain/settlement.md`「清算案」 | 2026-08-10 |
| グループ作成者の表示名をどう決めるか | User の名前を初期値として自動設定し、後から変更できる | `domain/group.md`「グループを作成する」 | 2026-08-10 |
| `domain/` の文書分割 | グループ / 金額と通貨 / 記録 / 差額と清算案 の 4 文書に分ける | `docs/README.md` 構成ツリー | 2026-08-10 |
| ターゲット | 旅行・パーティーなど、その場限りのイベントで一時的に集まったグループ | `overview.md`「ターゲット」 | 2026-08-10 |
| プラットフォーム | スマホ前提の Web アプリ。加えて Discord アプリとしても同機能を提供する | `overview.md`「前提」 | 2026-08-10 |
| 対応地域・言語 | 日本語のみ・日本国内が中心。ただし海外旅行で発生する外貨は扱う | `overview.md`「前提」 | 2026-08-10 |
| 収益・開発体制 | 無料・個人開発 | `overview.md`「前提」 | 2026-08-10 |
| User が持つ属性 | グループをまたいで変わらない同一性と、名前を持つ | `domain/group.md`「User の属性」 | 2026-08-10 |
| 参加コードを手入力させるか | させない。共有リンクに埋め込んで渡す。人が読み上げられる必要はない | `domain/group.md`「Group の属性」 | 2026-08-10 |
| 参加コードの形式・桁数 | cuid2 の既定設定（英小文字と数字・24 文字）で生成する | `adr/0002-invite-code.md` | 2026-08-10 |
| グループ名・既定通貨を後から変更できるか | どちらも変更できる。既定通貨を変えても過去の記録は変わらない | `domain/group.md`「グループ設定を変更する」 | 2026-08-10 |
| 対応する通貨の範囲と最小単位 | ISO 4217 の現行通貨。最小単位の桁数も ISO 4217 に従う | `domain/money.md`「扱う通貨」 | 2026-08-10 |
| 金額の上限 | 1 件の記録につき、その通貨の最小単位で 1,000,000,000 を上限とする。合計には上限を設けない | `domain/money.md`「金額の上限」 | 2026-08-10 |
| Payment の「内容」は必須か任意か | 任意。空でもよい | `domain/record.md`「Payment の属性」 | 2026-08-10 |
| 記録の並び順 | 発生日の新しい順。発生日が同じものは、後から登録したものを先に置く | `domain/record.md`「記録の並び」 | 2026-08-10 |
| 発生日に未来の日付を許すか | 許す。差額の計算は変わらない | `domain/record.md`「発生日」 | 2026-08-10 |
| 送金件数を「できるだけ少なく」する求め方 | 差額の絶対値が最大の負と最大の正を順に相殺する貪欲法 | `adr/0001-settlement-greedy.md` | 2026-08-10 |

import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // `next dev` が CLAUDE.md / AGENTS.md に自分の説明文を書き足すのを止める。
  // このリポジトリの CLAUDE.md は開発の規約そのものであり、道具が自動で書き換える場所ではない。
  agentRules: false,

  experimental: {
    /**
     * **直前に見たものを 30 秒だけ再利用する**
     * （`docs/adr/0009-web-ui.md`「直前に見たものを取り直さない」）。
     *
     * **ここで許すのはブラウザの中だけに存在するものである。** 他の利用者と共有される経路を
     * 持たないため、同 ADR「リクエストをまたぐキャッシュを持たない」が禁じた「人ごとに鍵を
     * 分ける」形にあたらない（同「この節が対象とするのはサーバー側で共有されるキャッシュである」）。
     *
     * **2 つとも 30 にするのは、窓が 1 つだからである。** 手元にあるものの出どころは 2 通りあり、
     * 既定のままだと窓の長さが揃わない。
     *
     * | | 何の窓か | 既定のまま置いたときに起きること |
     * |---|---|---|
     * | `dynamic` | 実際に見た画面を、もう一度出すまで | 0 秒。**戻るだけでも毎回取り直す** |
     * | `static` | 先読みで取れたものを、出すまで | このアプリの画面は何も cache しないため上限が付かず、**いつまでも古いものを出す** |
     *
     * **長く取らない理由**は同 ADR「検討した代替案」にある。古くなり得るのは他の人が別の端末で
     * 加えた変更だけで、自分の操作は手元にあるものを捨てさせる（`app/_lib/action.ts`）。
     */
    staleTimes: { dynamic: 30, static: 30 },
  },
}

export default nextConfig
